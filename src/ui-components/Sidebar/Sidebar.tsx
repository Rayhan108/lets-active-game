

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const Sidebar = ({toggleSidebar,isOpen}) => {
    return (
        <div>
            <p className="text-black">This is the sidebar content.</p>
        </div>
    );
};

export default Sidebar;