

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const MainHeader = ({toggleSidebar}) => {
    return (
        <div>
            <p className="text-black">This is the main header content.</p>
        </div>
    );
};

export default MainHeader;